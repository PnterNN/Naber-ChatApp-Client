<Window x:Class="JavaProject___Client.MVVM.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JavaProject___Client"
        xmlns:viewmodel="clr-namespace:JavaProject___Client.MVVM.ViewModel"
        mc:Ignorable="d"
        Title="Naber" Height="650" Width="1200"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        Background="Transparent"
        AllowsTransparency="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="25*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" MouseDown="Border_MouseDown"  Background="#4E72AB" CornerRadius="10,10,0,0">
            
        </Border>
        <ContentControl Grid.Row="1" Content="{Binding Navigation.CurrentView}"/>
    </Grid>
</Window>
